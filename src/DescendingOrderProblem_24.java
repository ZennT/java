public class DescendingOrderProblem_24 {
    /**
     * Ask from the user, how many numbers they want to sort
     * (That should be the size of your array)
     * Ask user to enter all the numbers they want to sort. Store them in an array you created
     * Sort that array in a descending order (without using Arrays.sort method)
     * Print the sorted array
     *  Hint: 1) Use Scanner class to get user input
     *        2) Use Arrays.copyOf to create a copy of an array to be sorted
     */
}
